
<div class="main">
  <div class="common-container">
    <ul class="posts-index-item">

    <% @posts.each do |post| %>
        <li class="card card-skin">
          <%# 投稿詳細へ飛ぶようにしたい %>
          <a href="/posts/<%= post.id %>">
            <div class="card__imgframe"></div>
            <div class="card__textbox">
                <div class="card__user-name">
                  <%= post.user.name %>
                </div>
              <div class="card__overviewtext">
                タイトル：<%= post.title %>
              </div>
            </div>
          </a>
        </li>

    <% end %>
    </ul>
  </div>
</div>
