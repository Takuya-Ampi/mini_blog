<div class="users-new-content">
  <div class="main">
    <div class="container">
      <h1 class="form-heading">新規登録</h1>
      <% @user.errors.full_messages.each do |message| %>
        <div class="form-error">
          <%= message %>
        </div>
      <% end %>
      <%= form_tag("/users/create", {multipart: true}) do %>
        <div class="form">
          <input type="text" placeholder="Name" name="name" value="<%= @user.name %>" required>
          <p class="text">アイコン画像を選択して下さい（任意)</p>
          <input class="input-image" name="image_name" type="file">
          <input type="email" placeholder="Email" name="email" value="<%= @user.email %>" required>
          <input class="password" type="password" placeholder="Password" name="password" value="<%= @user.password %>" required>
          <div class="btn-section">
            <input class="submit-btn" type="submit" value="新規登録">
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
